[project]
name = "release-notes-mcp"
version = "0.1.0"
description = "A small, generic MCP server for combining GitHub releases into product release notes"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
]

[project.scripts]
release-notes-mcp = "server:mcp.run"
